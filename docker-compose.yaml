version: '3.3'

services:
  klustair-frontend:
    container_name: klustair-frontend
    build:
      context: .
      dockerfile: Dockerfile
    image: klustair-frontend:latest
    restart: on-failure
    environment:
      - APP_NAME=${APP_NAME}
      - APP_ENV=${APP_ENV}
      - APP_KEY=${APP_KEY}
      - APP_DEBUG=${APP_DEBUG}
      - APP_URL=${APP_URL}
      - LOG_CHANNEL=${LOG_CHANNEL}
      - DB_CONNECTION=${DB_CONNECTION}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_DATABASE=${DB_DATABASE}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - BROADCAST_DRIVER=${BROADCAST_DRIVER}
      - CACHE_DRIVER=${CACHE_DRIVER}
      - QUEUE_CONNECTION=${QUEUE_CONNECTION}
      - SESSION_DRIVER=${SESSION_DRIVER}
      - SESSION_LIFETIME=${SESSION_LIFETIME}
      - ANCHORE_API_URL=${ANCHORE_API_URL:-http://host.docker.internal:8228}
      - ANCHORE_CLI_USER=${ANCHORE_CLI_USER:-admin}
      - ANCHORE_CLI_PASS=${ANCHORE_CLI_PASS:-foobar}
    ports:
      - 8080:80
    volumes:
      - ./:/var/www